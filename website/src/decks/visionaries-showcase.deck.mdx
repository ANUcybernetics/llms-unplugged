---
title: Demystifying large language models
description:
  A 14-minute talk for the ANU Visionaries Among Us Showcase (15 Aug 2026), in
  which a language model is trained and run live on stage from a two-book
  corpus, with a Wacom tablet, a stack of cards and a bag of balls
listed: false
---

import BigramCountsTable from "../components/BigramCountsTable.svelte";
import GridRowToggle from "../components/GridRowToggle.astro";
import GridZoom from "../components/GridZoom.svelte";
import SocyLogo from "astro-theme-anu/components/SocyLogo.astro";
import { getBigrams, splitTokens } from "../lib/tokens";
import "../styles/widgets.css";

export const HEMINGWAY =
  "the old man had taught the boy to fish and the boy loved him . no , the old man said .";
export const SEUSS =
  "and i said , how i wish we had something to do ! so we sat in the house .";
export const HYBRID_VOCAB =
  "the old man had taught boy to fish and loved him . no , said i how wish we something do ! so sat in house";

{/*
  Ben's 14-minute slot (3:46 pm, hard stop 4:00 pm for the stage changeover) at
  the ANU Visionaries Among Us Showcase, Manning Clark Hall, 15 Aug 2026.
  Program title: "Demystifying Large Language Models". Fuller plan (beats,
  corpus analysis, risks, Kenyon logistics): nb note anu-visionaries-showcase-2026.
  Deck work tracked in backlog TASK-140.

  Running order (target 13:00 against the 14:00 slot):
    0:00  Smallest thing on the program      (slides 1-2,   1:15)
    1:15  The promise                        (slide 3,      0:45)
    2:00  Training, live                     (slides 4-5,   3:15)
    5:15  Generation                         (slides 6-7,   3:30)
    8:45  The reading, then the reveal       (slides 8-9,   1:00)
    9:45  What everything was                (slide 10,     0:45)
   10:30  Scale                              (slides 11-13, 1:30)
   12:00  Human-scale AI, 80 years           (slides 14-15, 1:00)

  The whole talk runs off this deck on Ben's laptop over HDMI, Wacom attached.
  The AV company has exactly one cue: switch to Ben's feed at walk-on. All
  grid-drawing happens ON the deck via the whiteboard annotation surface:
  press shift-W over slide 4 (the empty grid) and tally with the pen; arrow
  keys still navigate with the surface up, and ink is committed per-slide, so
  advancing to slide 5 shows the pre-built grid clean. Do NOT press Escape or
  W mid-beat --- closing the annotation layer discards its ink. During
  generation (slide 7), the annotation surface stays DOWN: tap a row with the
  pen to band it gold (GridRowToggle), tap again to clear --- no ink, so
  nothing to clear between rounds. D saves an annotated slide as a PNG if you
  want a souvenir of the live tallies.

  The reveal structure is load-bearing: the two source books are NEVER named
  before slide 9. The live tallies (first ~10 bigrams, all Hemingway) go down
  in gold; slide 5 arrives with a second colour already in it, unexplained.
  Park it with one line and move on --- the two-colour mystery is what the
  reveal cashes in. Slide 9 builds in two clicks (both quotes are fragments):
  gold/Hemingway first, then blue/Seuss --- two laughs, and Seuss (the bigger
  recognition hit) lands second.

  Volunteers are the MCs (Lee Constable and Weli Costa; confirm with Kenyon):
  one holds the 42-card token stack in order, one holds the current card and
  drops it to the floor on "next!", so the pair of visible cards is the
  sliding bigram window. During generation the second MC scribes each drawn
  word onto a fresh blank card. Rehearse the full loop at the 1 pm sound check.

  Before delivery:
    - 42 A3 token cards in corpus order, NUMBERED ON THE BACKS (a dropped
      stack is otherwise fatal), plus ~15 blank cards and a fat marker for the
      scribe
    - tub of balls in six colours (max 3 distinct continuations per row, so
      six is headroom; the `the` row needs 2+2+1), one opaque bag, and a
      small table centre-stage for both
    - lapel or headset mic --- both hands are busy for eight minutes
    - pen colour/size chosen and rehearsed on slide 4 at 1 pm; check tally
      legibility in the 26x26 cells from the back of the hall
    - fallback if the annotation surface misbehaves: slide 5 exists
      regardless, so training can be narrated over the finished grid

  Generation numbers (worked out in the nb note): rows with a genuine choice
  are `the` (old x2, boy x2, house), `,`, `and`, `had`, `said`, `to` (the six
  two-book bridge rows) plus `man`, `boy`, `i`, `we` within one book;
  everything else is deterministic --- no bag, "the model has no choice
  here". Cap the walk at 10-12 words, and cut it off mid-flow deliberately:
  the arbitrary stop is a teaching beat (the model would keep going forever
  --- it has no idea what a sentence is; someone has to pull the plug). A
  choice row costs ~25-30s through the full bag ritual, a deterministic row
  ~8s. Start at `the`: fullest bag on the board, and it can jump books
  immediately. The 1-in-320 dream output is
  "the old man said, how i wish we had something to do!" --- if it lands,
  stop and read it twice.

  Audience note: some attendees also saw the unplugged-age-of-ai talk on
  10 Aug. The divergences are deliberate --- that talk deals a pre-trained
  model across the room's chairs and generates by hands-up; this one builds
  the model from an empty grid and samples from a bag. The one shared visual
  is the GridZoom pair, whose final punchline is re-anchored to the hall
  rather than the campus so it doesn't repeat word-for-word.
*/}

{/* _class: hero */}

# Demystifying large language models

![bg](./assets/bg-title-wide.avif)

{/* notes:
Ann: 65,000 years. Brad & Brian: edge of the universe. I have twenty-six words.

Hands up: used one this week → keep up if you could explain it to a ten-year-old. Hold the silence.
*/}

---

{/* _class: impact */}

we got the tool

before we got the **story** about the tool

{/* notes:
Not too complicated --- too fast. Two stories on offer ("basically a mind" / "just autocomplete"): opposites doing the same job --- don't look inside.
*/}

---

{/* _class: impact */}

it's hard to have an opinion on a machine

you've never seen the **inside** of

{/* notes:
The reckless promise: twelve minutes, cards, balls, Lee and Weli --- built from nothing, then it writes. Not a diagram --- the thing, at human speed.

Position the MCs, tub on the table. DON'T name the books.
*/}

---

<BigramCountsTable
  vocab={splitTokens(HYBRID_VOCAB)}
  bigrams={[]}
  dense={true}
/>

{/* notes:
TRAINING SURFACE. ⇧W now; NO Esc until the beat is done (discards ink). Arrows still navigate.

"This is the entire machine --- nothing behind the curtain; this IS the curtain."

Sliding window: two visible cards = the bigram. Read pair, tally, "next!" --- card drops to the FLOOR. ~10 live: first three slow, then let it turn mechanical. This is what "training" means.
*/}

---

<BigramCountsTable
  vocab={splitTokens(HYBRID_VOCAB)}
  bigrams={getBigrams(splitTokens(HEMINGWAY))}
  bigramsB={getBigrams(splitTokens(SEUSS))}
  dense={true}
/>

{/* notes:
Fast-forward: "thirty-one more of exactly this --- here's where it ends up."

FLOOR LINE (point at the pile): the text is on the floor; the model kept only the counts. 37 cells, 95% empty. "Stored the internet" dies here.

Colours: "hold that thought." Nothing more.
*/}

---

## the algorithm

![bg right:40%](./assets/bg-algorithm-generation-2.avif)

1. find the **row** for the last word written
2. each tally in that row is a **candidate** for the next word
3. load the bag: **one ball per tally**
4. **draw** --- and write the word down

{/* notes:
Walk the four steps once. The bag is why every reply is different --- it IS the probability distribution; we're just going to hold it.
*/}

---

<GridRowToggle />

<BigramCountsTable
  vocab={splitTokens(HYBRID_VOCAB)}
  bigrams={getBigrams(splitTokens(HEMINGWAY))}
  bigramsB={getBigrams(splitTokens(SEUSS))}
  dense={true}
/>

{/* notes:
GENERATION SURFACE. TAP a row (pen, annotation surface DOWN) to band it gold; tap again to clear --- no whiteboard ink needed between rounds. C only clears ink, not row bands.

Start at `the` (2 blue old, 2 red boy, 1 yellow house). Colours assigned out loud, fresh each round --- hold each ball at shoulder height as it's named, count the loads aloud (the bag is invisible from the back of the hall); scribe cards each word. Deterministic rows at speed: "no bag --- no choice."

CAP 10--12 words, cut mid-flow: "it would keep going forever --- it doesn't know what a sentence is; someone has to pull the plug." Dream line lands → stop, read it twice.
*/}

---

{/* _class: impact */}

we just ran a **language model**

{/* notes:
Flat. Then the reading --- MCs hold the scribed cards in a line facing the hall so the room reads along; funeral-poem straight; the worse the sentence, the straighter.

Nobody wrote it; it's in no book. Then (gesture at the ink): one more thing this machine was never told.
*/}

---

## two books

{/* Single-line JSX on purpose: MDX parses multi-line JSX children as flow
    content, which splits the text into sibling <p>s that escape the styled
    wrapper (and its colour). Inline children only survive on one line. */}

<p class="corpus-a fragment">“the old man had taught the boy to fish and the boy loved him. no, the old man said.”<br /><span class="corpus-attribution">— Ernest Hemingway, <em>The Old Man and the Sea</em> (1952)</span></p>

<p class="corpus-b fragment">“and i said, how i wish we had something to do! so we sat in the house.”<br /><span class="corpus-attribution">— Dr. Seuss, <em>The Cat in the Hat</em> (1957)</span></p>

{/* notes:
Two clicks. "The gold ink you watched go down..." → CLICK (Hemingway; let it land). "And the colour I asked you to hold that thought about..." → CLICK (Seuss; the bigger laugh --- wait it out).

Never told there were two books --- one word back, so it steps across the seam without noticing. Every strange AI sentence = this, at scale.
*/}

---

## what everything was

![bg right:40%](./assets/bg-core-insight-portrait.avif)

the grid → the **parameters**

tallying → **training**

the bag → the **probability distribution**

the draw → **sampling**

the cards on the floor → the **training data**, spent

the cards in Weli's hand → **generation**

{/* notes:
At a clip. Land on: "a trillion parameters" = a very large pile of this grid.
*/}

---

{/* _class: centered */}

<GridZoom
  rows={7441}
  cols={7441}
  label="Frankenstein"
  detail="7,441 distinct words · 55 million cells"
  compareRows={26}
  compareCols={26}
  compareLabel="our grid"
/>

{/* notes:
One novel instead of two sentences. The gold speck = everything we built tonight.
*/}

---

{/* _class: centered */}

<GridZoom
  rows={100000}
  cols={100000}
  label="a real model's vocabulary"
  detail="100,000 tokens · 10 billion cells"
  compareRows={7441}
  compareCols={7441}
  compareLabel="Frankenstein"
  note="at a centimetre per cell: 1 km² of paper — about 800 of this hall"
/>

{/* notes:
Last step a picture survives. Frankenstein is now the speck. 1 km² of paper --- about 800 of this hall.
*/}

---

{/* _class: impact */}

**15 trillion** words of training text

**~1 trillion** numbers in the model

{/* notes:
Every word went past a counter, like tonight. What changed: they look thousands of words back, not one --- most of what the size buys, and why they cross their seams less clumsily than ours did. What didn't: still words in, words out --- the loop that just ran on this stage.
*/}

---

{/* _class: impact */}

slowed down and zoomed in,

even this machine fits at **human scale**

{/* notes:
The app shows you the output; tonight you saw the machine. 1946: the atom, then --- same job again. Human-scale AI; one Studio sentence (agents type, students judged on taste).

TIME CHECK on arrival: past 12:15 → drop the Studio sentence first. Tee-up + callback must survive.

Tee-up: Brad & Brian do the opposite --- and they have a fog machine. Callback: hands can go up now.
*/}

---

{/* _class: socy-logo */}

<SocyLogo />
<p class="socy-url">www.llmsunplugged.org</p>
