{/* _class: hero */}

# Training

![bg](./assets/bg-div-training.avif)

---

## The recipe

![bg right:40%](./assets/bg-algorithm-training.avif)

```notes
1. **tidy up** your text: lowercase everything, treat words and single
   punctuation marks (`. , ! ? ; :`) as separate tokens
2. **set up** your grid: first token goes in both the row &amp; column headers
3. **fill in** the grid: for each consecutive pair of tokens, add a tally mark
   in that cell (add new rows/columns as needed)
```

walk through your text and tally up which tokens follow which in a grid

---

## Example

![bg right:40%](./assets/bg-example.avif)

_"Run, Spot, run. See Spot run."_

after tidying up:

`run` `,` `spot` `,` `run` `.` `see` `spot` `run` `.`

---

## The empty grid

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={0} />

---

## Training: `run` &rarr; `,`

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={1} />

---

## Training: `,` &rarr; `spot`

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={2} />

---

## Training: `spot` &rarr; `,`

```notes
`,` is already in our vocabulary &mdash; no new column needed!
```

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={3} />

---

## Training: `,` &rarr; `run`

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={4} />

---

## Training: `run` &rarr; `.`

```notes
`.` is a new token &mdash; punctuation gets its own row and column too
```

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={5} />

---

## Complete model

```notes
with the rest of the text tallied the same way, the grid is complete. `run` &rarr; `.` came up twice, so its cell has two marks &mdash; those counts are what make some next-words more likely than others.
```

<StaticGrid tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} step={10} />

---

## A few more tips

![bg right:40%](./assets/bg-training-tips.avif)

the last token in one sentence is followed by the first token in the next (even
over the page)

you can ignore quotation/speech marks (`"`)

work in pairs and divide the labour however you like---but don't neglect the
tally marks

```notes
Three snags worth pre-empting. The sentence boundary isn't a stop: the text is one long stream, so the full stop is followed by whatever word comes next, page break or not. Quote marks are noise for our purposes --- skip them rather than giving them a row and column. And pairs work well if one person reads the text aloud while the other tallies, but swap around, and whatever the split, the marks are the model --- a pair that races through the reading and skimps on the tallying has nothing to generate from.
```

---

{/* _class: hero */}

# Training

![bg](./assets/bg-div-training.avif)

<Timer minutes={10} overlay />

```notes
Hand out grids; groups tally their own text. 10 min, circulate --- the usual snag is that a token only gets a new row and column the first time it appears.
```

---

## The _language_ of language models

![bg right:60%](./assets/bg-markov.avif)

- model
- token
- weights

```notes
Everyday sense first, then ours: a model is a fashion model or a model aeroplane --- here it's the thing that captures the patterns, your grid. A token is a bus token or a token gesture --- here it's one word or punctuation mark. Weights are what you lift at the gym, or what you weigh up before deciding --- here they're just the tally marks, the numbers that make some next words likelier than others. Point at a cell: when you hear a model has billions of weights, that's what's being counted.
```
