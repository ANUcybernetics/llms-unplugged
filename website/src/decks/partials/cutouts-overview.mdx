{/* _class: hero */}

# Cutouts

![bg](./assets/bg-cutouts-overview.avif)

---

## Start with a text

![bg right:40%](./assets/bg-paper-mechanism.avif)

> And I will eat them here and there. Say! I will eat them anywhere!
>
> I do so like green eggs and ham!

--- Dr Seuss, _Green Eggs and Ham_

---

## After preprocessing

lowercase, keep `.` and `!` as **tokens**:

<CutoutsOverview tokens={OVERVIEW_TOKENS} mode="tokens" />

---

## A pair of words is a cutout

<CutoutsOverview tokens={OVERVIEW_TOKENS} mode="example" highlight="green eggs" />

any two words sitting next to each other make one cutout---the first word, then
the one that comes after it

---

## Every pair becomes a cutout

<CutoutsOverview tokens={OVERVIEW_TOKENS} mode="flow" />

each cutout overlaps the next---a cutout ends with the word the next one starts
with

{/* notes:
This section used to slide an animated window across the text, one slide per
pair. The cutouts already show that overlap themselves (a cutout ends with the
word the next cutout starts with), so it's now three static slides: the tokens,
one pair highlighted as a cutout, then every pair as a cutout.
*/}

---

{/* _animate: pile */}

## Now they're just a pile

<CutoutsOverview tokens={OVERVIEW_TOKENS} mode="scatter" seed={7} />

cut them out and tip them onto the table---the same cutouts, no longer in order

---

{/* _animate: pile */}

## Now they're just a pile

<CutoutsOverview tokens={OVERVIEW_TOKENS} mode="scatter" seed={42} />

shuffle them however you like---the model is the pile, not the order

{/* notes:
Two "scatter" frames of the *same* cutouts with different seeds. Both carry
{/* _animate: pile *​/}, so Reveal.js auto-animate glides each cutout (matched by
its data-id="cut-N") from one arrangement to the next --- it genuinely looks
shuffled. This is the hinge into Training/Generation: the model is an unordered
bag you rummage through, so order stops mattering once the text is cut up. Add
more {/* _animate: pile *​/} + <CutoutsOverview mode="scatter" seed={N}/> slides
(any seed) for a longer shuffle.
*/}
