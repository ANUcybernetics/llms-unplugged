{/* _class: hero */}

# Generation

![bg](./assets/bg-chain.avif)

---

## Your goal

![bg right:40%](./assets/bg-bag-draw.avif)

write down a starting word, then repeat:

1. find the row for the last word you wrote
2. **load the bag**: one ball per mark, in the colour of its strip
3. **draw one** without looking --- its colour names the next word
4. write it down, tip the bag back into the tub, and hand over to whoever has
   the next row

```notes
Walk through the next slides slowly --- the mechanics are simple but the hand-over ("who has the next row?") is where groups stall. Give every group four jobs: a reader (finds the row), a loader, a drawer, and a writer. If you have a real bag and a visualiser, do the draws for real and use the slides as the backup.
```

---

{/* _animate: draw */}

## Start with a word

<LedgerPage text="I" />

<div class="ledger-step">
  <LedgerRow entry={ROW_I} firstRow={3} palette={PALETTE} />
  <LedgerBag entry={ROW_I} firstRow={3} palette={PALETTE} empty />
</div>

---

{/* _animate: draw */}

## Load the bag

<LedgerPage text="I" />

<div class="ledger-step">
  <LedgerRow entry={ROW_I} firstRow={3} palette={PALETTE} />
  <LedgerBag entry={ROW_I} firstRow={3} palette={PALETTE} />
</div>

```notes
Fifteen balls go in: eight pink for "am", five purple for "do", two black for "would". The bag now holds the row's counts --- the more marks, the more balls, the more likely.
```

---

{/* _animate: draw */}

## Draw one

<LedgerPage text="I would" fresh />

<div class="ledger-step">
  <LedgerRow entry={ROW_I} firstRow={3} palette={PALETTE} highlight={2} />
  <LedgerBag entry={ROW_I} firstRow={3} palette={PALETTE} drawn={2} />
</div>

```notes
Black: the two-in-fifteen chance came up. Unlikely isn't impossible --- that's the bag doing its job.
```

---

{/* _animate: draw */}

## Load the bag, draw one

<LedgerPage text="I would not" fresh />

<div class="ledger-step">
  <LedgerRow entry={ROW_WOULD} firstRow={2} palette={PALETTE} highlight={1} />
  <LedgerBag entry={ROW_WOULD} firstRow={2} palette={PALETTE} drawn={1} />
</div>

```notes
Four balls, two red and two blue: a coin toss between "you" and "not".
```

---

{/* _animate: draw */}

## Only one place to go

<LedgerPage text="I would not like" fresh />

<div class="ledger-step">
  <LedgerRow entry={ROW_NOT} firstRow={0} palette={PALETTE} highlight={0} />
</div>

no bag needed: after `not` this book only ever said `like`

---

{/* _animate: draw */}

## Load the bag

<LedgerPage text="I would not like" />

<div class="ledger-step">
  <LedgerRow entry={ROW_LIKE} firstRow={4} palette={PALETTE} />
  <LedgerBag entry={ROW_LIKE} firstRow={4} palette={PALETTE} />
</div>

---

{/* _animate: draw */}

## Draw one

<LedgerPage text="I would not like green" fresh />

<div class="ledger-step">
  <LedgerRow entry={ROW_LIKE} firstRow={4} palette={PALETTE} highlight={1} />
  <LedgerBag entry={ROW_LIKE} firstRow={4} palette={PALETTE} drawn={1} />
</div>

```notes
A blue ball, and the word beside the blue strip is "green". Say it out loud: the colour names the column, not the word. Someone always laughs here, which is the point.
```

---

{/* _animate: draw */}

## Three in a row with only one place to go

<LedgerPage text="I would not like green eggs and ham" fresh />

<div class="ledger-step ledger-stack">
  <LedgerRow entry={ROW_GREEN} firstRow={0} palette={PALETTE} highlight={0} />
  <LedgerRow entry={ROW_EGGS} firstRow={3} palette={PALETTE} highlight={0} />
  <LedgerRow entry={ROW_AND} firstRow={0} palette={PALETTE} highlight={0} />
</div>

```notes
Three hand-overs, no bag: "green" only ever went to "eggs", "eggs" to "and", "and" to "ham". The model is on rails through the book's favourite phrase.
```

---

{/* _animate: draw */}

## Load the bag, draw one

<LedgerPage text="I would not like green eggs and ham ." fresh />

<div class="ledger-step">
  <LedgerRow entry={ROW_HAM} firstRow={1} palette={PALETTE} highlight={1} />
  <LedgerBag entry={ROW_HAM} firstRow={1} palette={PALETTE} drawn={1} />
</div>

```notes
Three balls: one pink for "?", two purple for ".". Nine draws in, and we've got a sentence these hundred words never said. The book says "I would not like them here or there" and "I do not like green eggs and ham"; the model has glued them together, because it only ever knows what comes next, one word at a time. Stop at a full stop or a question mark you like, or keep going.
```

---

{/* _class: impact */}

_"I would not like green eggs and ham."_

a hint of the original, not a copy

---

## A word with lots of followers

<div class="ledger-step">
  <LedgerRow entry={ROW_A_BEAR} firstRow={3} palette={PALETTE} />
  <LedgerBag entry={ROW_A_BEAR} firstRow={3} palette={PALETTE} />
</div>

more than four followers spill onto the **row below**, where the word is printed
again in grey

the second row's strips take the **other four colours**, so every follower still
has its own --- load the bag from **both rows**

```notes
This is the Bear Hunt row for "a": eight balls from two rows, two pink, two purple, two black, one white, one red. Every book but Green Eggs has a row or two like this, so every group will meet one. The mistake to watch for is loading only the top row.
```

---

## You will need

![bg right:40%](./assets/bg-generation-supplies.avif)

**with your group**

your five sheets (one each), the bag, and the tub of balls

pen and paper for the story

**every group has a different book** --- start from any word on your sheets

```notes
Hand out the bags and tubs now. Five books in the room, so with more than five groups some share a book --- that's fine, and the shareback uses it. The row for "." is the honest place to start (it says how the book starts sentences), but any word will do.
```

---

## Activity

![bg right:40%](./assets/bg-activity-generation.avif)

generate as much story as you can --- when you hit a `.` or `?` you like, start
a new sentence from any word

```notes
Circulate. Common stalls: forgetting to tip the bag back into the tub between draws (the balls from the last row are still in there); loading only the top row of a two-row word; a row with no marks at all (the word only ever ended the text --- start again from any word); arguing about which strip a colour is (read the name printed in the strip's corner).
```

---

{/* _class: hero */}

# Generation

![bg](./assets/bg-chain.avif)

<Timer minutes={12} overlay />

---

{/* _class: hero */}

# Shareback

![bg](./assets/bg-shareback-generation.avif)

---

## Different books, different stories

![bg right:40%](./assets/bg-information-flow-generation.avif)

read out your first sentence --- can the rest of the room guess your book?

two groups with the **same book**: why aren't your stories the same?

could any group's model write a story about a **crocodile**?

```notes
Pick four or five groups, not all of them. Three probes. First: the stories sound like their books, because the model only has the words, and the pairs, that its book had. Second: two groups with the same sheets gave two different stories --- the bag is random, and a model that's asked twice gives two answers (the wrap-up comes back to this). Third: no crocodile anywhere on any sheet, so no model in the room can say it. Then the hinge: so where did these marks come from? Someone counted. Next section: you're the ones counting.
```
