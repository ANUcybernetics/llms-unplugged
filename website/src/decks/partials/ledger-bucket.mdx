{/* _class: hero */}

# One story, all together

![bg](./assets/bg-bucket.avif)

```notes
Optional section --- drop if running short on time. Butchers paper up the front for the class story; every group keeps its own sheets, bag and tub.
```

---

## Five texts, five models

![bg right:40%](./assets/bg-five-texts.avif)

each group trained on a different text, so each group's row for `the` is
different

together the class has a **bigger model** than any one group

```notes
Ask each group what came after "the" in their text. Nobody has "volcano" except the volcano group; nobody has "bus" except the bus group. The class as a whole knows all of them.
```

---

## The same word, five different rows

<div class="ledger-stack">
  <LedgerRow entry={ROW_THE_BELL} firstRow={4} palette={PALETTE} />
  <LedgerRow entry={ROW_THE_BUS} firstRow={5} palette={PALETTE} />
  <LedgerRow entry={ROW_THE_DOG} firstRow={7} palette={PALETTE} />
  <LedgerRow entry={ROW_THE_RAIN} firstRow={0} palette={PALETTE} />
  <LedgerRow entry={ROW_THE_VOLCANO} firstRow={0} palette={PALETTE} />
</div>

same word, five models, five different answers

```notes
Read down the followers and you can name the text from them: bell and key, bus and air, classroom and dog, roof and sun, volcano and foam. Nobody's row is wrong --- each one is an honest count of the text that group read.
```

---

## How we'll do it

![bg right:40%](./assets/bg-bucket.avif)

I'll call out the word we're on

**hands up** if your sheets have a row for it --- I'll pick a group

that group loads their bag from that row and draws: their word is the **class's
next word**

```notes
Two steps per word: the show of hands picks the group, the group's bag picks the word. Spread the picks around --- a group with a rare word is worth choosing when you want the class story to surprise people. Write each word on the butchers paper as it lands, and the word you just wrote is the next one you call.
```

---

## That group draws the word

<div class="ledger-step">
  <LedgerRow entry={ROW_THE_VOLCANO} palette={PALETTE} highlight={1} />
  <LedgerBag entry={ROW_THE_VOLCANO} palette={PALETTE} drawn={1} />
</div>

the class story grows by one word --- and the new word is the next one we hunt
for

```notes
Six counters, and blue comes out: "volcano". Keep going for a few sentences; a word only one group has (volcano, bus, dog) is the fun moment --- the class model can say things most groups never saw.
```

---

{/* _class: hero */}

# One story, all together

![bg](./assets/bg-bucket.avif)

<Timer minutes={10} overlay />
