{/* _class: hero */}

# Generation

![bg](./assets/bg-div-lessons.avif)

---

## The recipe

![bg right:40%](./assets/bg-algorithm-generation.avif)

{/* notes:
<ol>
<li><strong>choose</strong> a starting word from your grid (any word --- not just the first one)</li>
<li><strong>look</strong> at that word's row to find all possible next words and their counts</li>
<li><strong>roll dice</strong> weighted by the counts to pick the next word</li>
<li><strong>write down</strong> the chosen word and make it your new starting word</li>
<li><strong>repeat</strong> from step 2</li>
</ol>
*/}

use your grid to generate new text, rolling dice to choose each next word

---

## Generation: start with `see`

{/* notes: any word will do as a seed --- we're starting from `see`, which is the last row, to show you don't have to begin at the top. Only one option here, so no roll. */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={0} />

---

## Generation: from `spot`

{/* notes: two options in this row (`,` and `run`) with equal tallies --- highlight both, then roll */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={1} phase="preroll" />

---

## How the die chooses: `spot`

{/* notes: equal tallies, so each option gets an equal share of the ten d10 faces --- 0-4 for one, 5-9 for the other. We rolled a 2. */}

<DiceStrip tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} currentWord="spot" rolled={2} />

---

## Generation: `spot` → `,`

{/* notes: the 2 landed in the `,` band, so `,` is our next word */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={1} />

---

## Generation: from `,`

{/* notes: another two-option row (`spot` and `run`), still equal --- both highlighted, then roll */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={2} phase="preroll" />

---

## Generation: `,` → `run`

{/* notes: rolled a 7, which lands in the `run` band */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={2} />

---

## Generation: from `run`

{/* notes: two options again --- but `.` has two tallies to `,`'s one, so this isn't a 50/50 roll */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={3} phase="preroll" />

---

## How the die chooses: `run`

{/* notes: `.` has twice the tallies, so it gets more of the faces (0-6) than `,` (7-9). With a ten-sided die that's the closest split rather than exactly 2:1 --- the point is more tallies → more faces → more likely. We rolled a 3. */}

<DiceStrip tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} currentWord="run" rolled={3} />

---

## Generation: `run` → `.`

{/* notes: the 3 sits in the `.` band, so we pick `.` */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={3} />

---

## Generation: from `.`

{/* notes: only one option (`see`) --- no roll. Note we keep rolling straight past the full stop; the model has no idea a sentence just ended. */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={4} />

---

## Generation: back to `see`

{/* notes: and so on --- we've looped back to where we started. Keep going for as long as you like; you decide when to stop. */}

<StaticGeneration tokens={EXAMPLE_TOKENS} vocabulary={EXAMPLE_VOCAB} sequence={EXAMPLE_GENERATION} rolls={EXAMPLE_GENERATION_ROLLS} step={5} />

---

## Generated text

![bg right:40%](./assets/bg-generated-text.avif)

_"see spot, run. see"_

a new sentence --- not in the training data!

---

{/* _class: hero */}

# Generation

![bg](./assets/bg-div-lessons.avif)

<Timer minutes={10} overlay />

{/* notes:
Groups generate from their grid, rolling dice on each choice. 10 min, circulate. Where a row has only one option, no roll needed.
*/}

---

{/* _class: hero */}

# Shareback

![bg](./assets/bg-div-reception.avif)

{/* notes:
Take a couple of groups' generated sentences; celebrate the weird ones. ~5 min.
*/}

---

## The _language_ of language models

![bg cover left:60%](./assets/bg-shannon.avif)

- prompt
- response/completion
- context window

{/* notes:
Gloss each: prompt = the starting text you give it; response/completion = the text it generates back; context window = how much it can "see" at once (here, just the current word).
*/}
