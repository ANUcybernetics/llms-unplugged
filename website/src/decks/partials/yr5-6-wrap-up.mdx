## About your stories

![bg right:40%](./assets/bg-participants.avif)

{/* prettier-ignore */}
<p class="fragment">if you started with the <strong>same starting word</strong>, would you generate the same story again?</p>

{/* prettier-ignore */}
<p class="fragment">what would help our paper language models write <strong>better</strong> stories?</p>

{/* prettier-ignore */}
<p class="fragment">what is <strong>different</strong> about the way you write a story and the way a language model does?</p>

```notes
First: no---re-run a generation, or the Part 1 chatbot story, to show the randomness gives a different answer every time. Second opens the door to "more data, bigger model, longer memory" without naming those terms. Third is the one to leave time for: a person writes with something to say, the whole story in their head, and a reader in mind; the model only ever picks the next word from the one before it. Take answers rather than supplying that one.
```

---

{/* _class: hero */}

# Questions?

![bg](./assets/bg-what-next.avif)
