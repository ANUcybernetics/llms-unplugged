{/* _if: presenters */}

{/* Headshot grid, gated behind `?presenters`. The param's value selects who
appears by first initial -- `?presenters=ceb` -> Cole, Eddie, Ben; a bare
`?presenters` shows everyone. See PresenterGrid.astro for the roster and the
column-sizing logic. */}

<PresenterGrid />
