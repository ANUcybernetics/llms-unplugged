---
title: LLMs Unplugged — How AI writes stories (ledger)
description:
  A 90-minute school workshop (ages 10-16) using paper ledger sheets and a bag
  of coloured counters to discover how AI writes stories
---

import { LEDGER_PALETTE } from "../lib/ledger";
import LedgerToken from "../components/LedgerToken.astro";
import LedgerRow from "../components/LedgerRow.svelte";
import LedgerBag from "../components/LedgerBag.svelte";
import LedgerPage from "../components/LedgerPage.svelte";
import LedgerTraining from "../components/LedgerTraining.svelte";
import LedgerSheet from "../components/LedgerSheet.svelte";
import LedgerSheets from "../components/LedgerSheets.svelte";
import Timer from "../components/Timer.astro";
import PresenterGrid from "../components/PresenterGrid.astro";
import SocyLogo from "astro-theme-anu/components/SocyLogo.astro";
import BgMusic from "../components/BgMusic.astro";
import CopyButton from "../components/CopyButton.astro";
import { STORY_FIELDS, STORY_PROMPT } from "../lib/storyPrompt";
import "../styles/widgets.css";
import "../styles/ledger.css";
import "./how-ai-writes-stories-ledger.deck.css";

<BgMusic />

export const components = { code: LedgerToken };

```comment
Every group generates from a different picture book (see the lesson entry for
   the five sets), and the slides walk through one of them: every ROW_* below
   is a real row of the Green Eggs and Ham set, the first 100 tokens dealt
   across 5 sheets, printed for a room with counters in eight colours ---
   `llms_unplugged ledger -i data/green-eggs-and-ham.txt --max-tokens 100
   --sheets 5 --palette @cli/ledger-palette-eight.json --prefill tallies`,
   whose palette is the first eight of the CLI's default
   (cli/ledger-palette.json), which is what PALETTE below slices. The firstRow
   passed to each widget is the row's physical position on its sheet, which is
   what decides its colours, so a regenerated set (a different --max-tokens, a
   different sheet count) means re-reading the rows and positions off the new
   sheets. SHEET_THREE is that set's third sheet entire, in the order the deal
   printed it. ROW_A_BEAR, for the parked two-row slide, comes from the Bear
   Hunt set (same command, data/were-going-on-a-bear-hunt.txt). The five
   ROW_THE_* are the school-day texts' rows for "the" (data/originals/school-day-*.txt),
   each set built with --prefill prefixes for training.
```

export const PALETTE = LEDGER_PALETTE.slice(0, 8);

```comment
Sheet 3 of the Green Eggs set, "green" to "like": the sheet the walkthrough
   opens on. Its rows sit at physical rows 0-4, which is where their colours
   come from, and "I" is row 3.
```

export const ROW_GREEN = {
  prefix: "green",
  followers: [{ text: "eggs", count: 3 }],
};

export const ROW_HAM = {
  prefix: "ham",
  followers: [
    { text: "?", count: 1 },
    { text: ".", count: 2 },
  ],
};

export const ROW_HERE = {
  prefix: "here",
  followers: [{ text: "or", count: 2 }],
};

export const ROW_I = {
  prefix: "I",
  followers: [
    { text: "am", count: 8 },
    { text: "do", count: 5 },
    { text: "would", count: 2 },
  ],
};

export const ROW_LIKE = {
  prefix: "like",
  followers: [
    { text: "that", count: 1 },
    { text: "green", count: 3 },
    { text: "them", count: 5 },
  ],
};

export const SHEET_THREE = [ROW_GREEN, ROW_HAM, ROW_HERE, ROW_I, ROW_LIKE];

```comment
The rest of the generation walkthrough, off the other sheets:
   "I am ! would you like them anywhere ." --- a sentence stitched from three
   the book actually says. Row positions: am 4, ! 0 (sheet 1); anywhere 1
   (sheet 2); would 2, you 3, them 0 (sheet 5).
```

export const ROW_AM = {
  prefix: "am",
  followers: [
    { text: "Sam", count: 2 },
    { text: ".", count: 2 },
    { text: "!", count: 3 },
  ],
};

export const ROW_BANG = {
  prefix: "!",
  followers: [
    { text: "that", count: 1 },
    { text: "I", count: 1 },
    { text: "would", count: 1 },
  ],
};

export const ROW_WOULD = {
  prefix: "would",
  followers: [
    { text: "you", count: 2 },
    { text: "not", count: 2 },
  ],
};

export const ROW_YOU = {
  prefix: "you",
  followers: [{ text: "like", count: 2 }],
};

export const ROW_THEM = {
  prefix: "them",
  followers: [
    { text: ",", count: 2 },
    { text: "here", count: 2 },
    { text: "anywhere", count: 1 },
  ],
};

export const ROW_ANYWHERE = {
  prefix: "anywhere",
  followers: [{ text: ".", count: 1 }],
};

export const SHEET_RANGES = [
  ["!", "am"],
  ["and", "eggs"],
  ["green", "like"],
  ["not", "that"],
  ["them", "you"],
];

export const ROW_A_BEAR = {
  prefix: "a",
  followers: [
    { text: "bear", count: 2 },
    { text: "big", count: 2 },
    { text: "beautiful", count: 2 },
    { text: "river", count: 1 },
    { text: "deep", count: 1 },
  ],
};

export const GE_OPENING =
  "I am Sam . I am Sam . Sam I am . that Sam I am ! that Sam I am ! I do not like that Sam I am !";

```comment
The five school-day texts' rows for "the", for the finale: same word, five
   different models. Physical rows: bell 4, bus 5, dog 7, rain 0, volcano 0.
```

export const ROW_THE_BELL = {
  prefix: "the",
  followers: [
    { text: "bell", count: 2 },
    { text: "door", count: 3 },
    { text: "teacher", count: 2 },
    { text: "key", count: 1 },
  ],
};

export const ROW_THE_BUS = {
  prefix: "the",
  followers: [
    { text: "bus", count: 5 },
    { text: "floor", count: 1 },
    { text: "air", count: 1 },
  ],
};

export const ROW_THE_DOG = {
  prefix: "the",
  followers: [
    { text: "classroom", count: 1 },
    { text: "teacher", count: 2 },
    { text: "dog", count: 2 },
    { text: "door", count: 1 },
  ],
};

export const ROW_THE_RAIN = {
  prefix: "the",
  followers: [
    { text: "rain", count: 2 },
    { text: "roof", count: 1 },
    { text: "sun", count: 1 },
    { text: "teacher", count: 1 },
  ],
};

export const ROW_THE_VOLCANO = {
  prefix: "the",
  followers: [
    { text: "teacher", count: 3 },
    { text: "volcano", count: 1 },
    { text: "desk", count: 1 },
    { text: "floor", count: 1 },
  ],
};

```comment
90-minute workshop for students aged 10-16, in groups of 4-5. Generation comes
before training on purpose: a group that has drawn words out of a finished
sheet knows what the marks are for before it makes any. Seven parts:
  Part 1 (12 min)  What is AI? (yr5-6-what-is-ai --- groups form here)
  Part 2 (8 min)   Warm-up + words (yr5-6-pattern-warmup + yr5-6-definitions)
  Part 3 (5 min)   Read the sheet (ledger-read)
  Part 4 (20 min)  Generation + shareback (ledger-generation)
  Part 5 (22 min)  Training on a new text, then a short generation (ledger-training)
  Part 6 (10 min)  One story, all together (ledger-bucket --- drop if running over)
  Part 7 (5 min)   Wrap-up + Q&A (yr5-6-wrap-up)
Allow 5 min for arrival/settle/introductions before Part 1.

The sets are printed with four counter colours, and every text is chosen so
that no word has more than four followers: one row per word, no continuation
rows. The slides for the two-row case are parked in ledger-generation, ready
for a set that needs them.

Before each delivery:
- print the pre-tallied picture-book sets (5 sheets each, plus the brief), a
  different book per group; print the school-day sets with --prefill
  prefixes, a different text per group, with their text.pdf pages
- counters in the palette's colours, at least 8 of each per group (the largest
  tally in any set), one bag and one tub per group
- a few "predict the next word" prompts for the whiteboard
```

{/* _class: hero */}

# LLMs Unplugged

## How AI writes stories

![bg](./assets/bg-ledger-title.avif)

```notes
Open warm: today we'll find out how AI writes stories, and write some ourselves with paper and a bag of counters. No computers.
```

---

{/* @include ./partials/ack-country.mdx */}

---

{/* @include ./partials/presenters.mdx */}

---

{/* @include ./partials/yr5-6-what-is-ai.mdx */}

---

{/* @include ./partials/yr5-6-pattern-warmup.mdx */}

---

{/* @include ./partials/yr5-6-definitions.mdx */}

---

{/* @include ./partials/ledger-read.mdx */}

---

{/* @include ./partials/ledger-generation.mdx */}

---

{/* @include ./partials/ledger-training.mdx */}

---

{/* @include ./partials/ledger-bucket.mdx */}

---

{/* @include ./partials/yr5-6-wrap-up.mdx */}

---

{/* _class: socy-logo */}

<SocyLogo />
<p class="socy-url">www.llmsunplugged.org</p>
