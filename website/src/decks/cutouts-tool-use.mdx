{/* _class: banner */}

# Agentic tool use

![bg brightness:0.5](./assets/bg-paper-mechanism.avif)

---

## What makes a model an "agent"?

![bg right:40%](./assets/bg-insight.avif)

an agent is a model that can **call tools**: pause generation, get information
from outside, and continue

the loop: generate → trigger word → call tool → write the result → keep going

---

## Trigger words and tools

| Trigger                    | Tool            | Returns                       |
| -------------------------- | --------------- | ----------------------------- |
| <Token next>VOTE</Token>   | the room        | yes / no (majority)           |
| <Token next>ACTION</Token> | a chosen person | a word for what they just did |

---

## The activity

add new cutouts to your spread:

- <Cutout prev="the" next="VOTE" /> <Cutout prev="is" next="VOTE" />
  <Cutout prev="we" next="ACTION" />
- <Cutout prev="VOTE" next="." /> <Cutout prev="ACTION" next="." /> (so the
  result fits back into the chain)

designate a tool operator---when generation hits <Token next>VOTE</Token>, pause and ask the
room; on <Token next>ACTION</Token>, the chosen person does something and reports back

---

## Activity (~15 min)

![bg right:40%](./assets/bg-group-activity.avif)

1. add tool-use cutouts to your spread
2. generate---when you hit a trigger word, hand off to the tool operator
3. write the operator's answer down as the next word(s)
4. continue generating

---

## Connection to current LLMs

![bg right:40%](./assets/bg-information-flow.avif)

modern AI agents do exactly this: web search, code execution, database
queries, robot control

the model doesn't _know_ the answer---it just learned _when to ask_

real-world agents loop through this hundreds of times to complete a task

---

{/* _class: banner */}

# Shareback

![bg brightness:0.5](./assets/bg-shareback.avif)
