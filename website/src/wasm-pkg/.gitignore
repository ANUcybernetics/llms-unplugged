# wasm-bindgen output - rebuild with:
#   cd cli && pnpm dlx wasm-pack build --target web --out-dir ../website/src/wasm-pkg --no-default-features --features wasm
# These files are checked in so CI can bundle them in the website build.
# Note: wasm-pack regenerates this file as "*" on each build.
